experiment_name: qwen3-17b-aime-hardmath-correct-grpo-b200-dev-300
trial_name: trial0

seed: 7
enable_offload: false
total_train_epochs: 10
total_train_steps: 300
tokenizer_path: ${actor.path}

cluster:
  n_nodes: 1
  n_gpus_per_node: 4
  fileroot: /NHNHOME/areal_runs/qwen3-gsm8k-rlvr
  name_resolve:
    type: nfs
    nfs_record_root: /NHNHOME/areal_runs/qwen3-gsm8k-rlvr/name_resolve

scheduler:
  type: local

rollout:
  backend: "sglang:d2p1t1"
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  max_concurrent_rollouts: 256
  queue_size: 4096
  consumer_batch_size: ${train_dataset.batch_size}
  max_head_offpolicyness: 2
  enable_rollout_tracing: false
  scheduling_spec: ${actor.scheduling_spec}
  fileroot: ${cluster.fileroot}
  tokenizer_path: ${tokenizer_path}
  dump_to_file: true

gconfig:
  n_samples: 8
  min_new_tokens: 0
  max_new_tokens: 1024
  greedy: false
  temperature: 0.6
  top_p: 0.95
  top_k: 20

eval_gconfig:
  n_samples: 1
  min_new_tokens: 0
  max_new_tokens: 1024
  greedy: false
  temperature: 0.6
  top_p: 0.95
  top_k: 20

actor:
  backend: "megatron:d2p1t1"
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  path: Qwen/Qwen3-1.7B
  init_from_scratch: false
  disable_dropout: true
  gradient_checkpointing: false
  dtype: bfloat16
  mb_spec:
    max_tokens_per_mb: 32768
    packing_algorithm: ffd
  optimizer:
    type: adam
    lr: 1e-6
    weight_decay: 0.01
    beta1: 0.9
    beta2: 0.999
    eps: 1e-8
    lr_scheduler_type: constant
    gradient_clipping: 1.0
    warmup_steps_proportion: 0.001
  eps_clip: 0.25
  temperature: ${gconfig.temperature}
  reward_scaling: 10.0
  reward_bias: -0.5
  kl_ctl: 0.0
  ppo_n_minibatches: 1
  recompute_logprob: true
  use_decoupled_loss: true
  rejection_sampling:
    metric: ratio
    upper: 5.0
  reward_norm:
    mean_level: group
    std_level: group
    group_size: ${gconfig.n_samples}
  adv_norm:
    mean_level: batch
    std_level: batch
  weight_update_mode: xccl
  max_new_tokens: ${gconfig.max_new_tokens}
  megatron:
    bridge_type: mbridge
    ddp:
      overlap_grad_reduce: false
      overlap_param_gather: false
      use_distributed_optimizer: true
    use_deterministic_algorithms: false
  scheduling_spec:
    - task_type: worker
      port_count: 2
      gpu: 1
      cpu: 12
      mem: 96
      cmd: python3 -m areal.infra.rpc.rpc_server
      env_vars:
        PATH: /NHNHOME/PROJECT/wbl-workspace/ewer/rl-test/AReaL/.venv/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
        NCCL_SOCKET_IFNAME: eth0
        GLOO_SOCKET_IFNAME: eth0
        NCCL_CUMEM_ENABLE: "0"
        NCCL_NVLS_ENABLE: "0"
        CUDA_DEVICE_MAX_CONNECTIONS: "1"
        PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True
        HF_HOME: /NHNHOME/areal_cache/huggingface
        HF_HUB_ENABLE_HF_TRANSFER: "1"
        TRITON_CACHE_DIR: /NHNHOME/areal_cache/triton
        TRITON_CACHE_PATH: /NHNHOME/areal_cache/triton
        TRANSFORMERS_NO_TF: "1"
        USE_TF: "0"
        USE_FLAX: "0"
        TF_CPP_MIN_LOG_LEVEL: "3"

sglang:
  model_path: ${actor.path}
  random_seed: ${seed}
  skip_tokenizer_init: true
  dtype: ${actor.dtype}
  max_running_requests: 128
  context_length: 4096
  mem_fraction_static: 0.60
  attention_backend: flashinfer
  log_level: warning
  log_level_http: warning
  chunked_prefill_size: -1
  max_prefill_tokens: 4096

train_dataset:
  batch_size: 16
  shuffle: true
  pin_memory: true
  num_workers: 4
  path: mixed_math
  split: train
  type: rl
  max_length: 2048
  scheduling_spec: null
  dataset_kwargs:
    reward_fn: rlvr_demo.multi_math_reward.qwen3_math_correct_reward_fn
    shuffle_records: true
    sources:
      - name: aime_historical
        split: train
        max_year: 2021
      - name: aime_recent
        split: train
        min_year: 2022
        max_year: 2023
      - name: math
        split: train
        levels: ["Level 3", "Level 4", "Level 5"]
    eval_sources:
      - name: aime_historical
        split: train
        min_year: 2024
        max_year: 2024
      - name: aime_2025
        split: train

valid_dataset:
  batch_size: 60
  shuffle: false
  pin_memory: true
  num_workers: 4
  path: mixed_math
  split: train
  type: rl
  max_length: 2048
  scheduling_spec: null
  dataset_kwargs:
    reward_fn: rlvr_demo.multi_math_reward.qwen3_math_correct_reward_fn
    shuffle_records: false
    sources:
      - name: aime_historical
        split: train
        min_year: 2024
        max_year: 2024
      - name: aime_2025
        split: train

saver:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  freq_epochs: null
  freq_steps: 50
  freq_secs: null

recover:
  mode: disabled
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  freq_epochs: null
  freq_steps: null
  freq_secs: null

evaluator:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  eval_before_train: true
  freq_epochs: null
  freq_steps: 50
  freq_secs: null

stats_logger:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  wandb:
    mode: disabled

perf_tracer:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  enabled: false
  session_tracer:
    enabled: false
